@model Company.Data.Data.Card

@{
    ViewData["Title"] = "Delete";
}

<!-- Sekcja z tabelą z ograniczoną szerokością -->
<section class="table-section">

<div class="container mt-5">
    <h1 class="text-danger">
        <i class="fas fa-trash-alt"></i> Delete Card
    </h1>
    <p class="lead">Are you sure you want to delete this card? This action cannot be undone.</p>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">
                <i class="fas fa-exclamation-triangle"></i> Card Details
            </h4>
        </div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-credit-card"></i> @Html.DisplayNameFor(model => model.CardNumber)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.CardNumber)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-calendar-alt"></i> @Html.DisplayNameFor(model => model.ExpiryDate)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.ExpiryDate)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-id-card"></i> @Html.DisplayNameFor(model => model.CardType)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.CardType.Name)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-user"></i> @Html.DisplayNameFor(model => model.CardHolder)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.CardHolder)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-shield-alt"></i> @Html.DisplayNameFor(model => model.CVV)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.CVV)
                </dd>
            </dl>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center">
        <form asp-action="Delete" class="d-inline">
            <input type="hidden" asp-for="IdCard" />
            <button type="submit" class="btn-danger">
                <i class="fas fa-trash"></i> Delete
            </button>
        </form>
        <a asp-action="Index" class="btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>
</div>

</section>

<!-- Font Awesome CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

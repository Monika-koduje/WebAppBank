@model Company.Data.Data.Client

@{
    ViewData["Title"] = "Details";
}

<!-- Sekcja z tabelą z ograniczoną szerokością -->
<section class="table-section">

<div class="container mt-5">
    <h1 class="text-primary">
        <i class="fas fa-user"></i> Client Details
    </h1>
    <p class="lead">Here are the details for the selected client.</p>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-info text-white">
            <h4 class="mb-0">
                <i class="fas fa-id-card"></i> Client Information
            </h4>
        </div>
        <div class="card-body">
            <dl class="row">
                <!-- First Name -->
                <dt class="col-sm-2">
                    <i class="fas fa-user-tag"></i> @Html.DisplayNameFor(model => model.FirstName)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.FirstName)
                </dd>

                <!-- Last Name -->
                <dt class="col-sm-2">
                    <i class="fas fa-user-tag"></i> @Html.DisplayNameFor(model => model.LastName)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.LastName)
                </dd>

                <!-- Email -->
                <dt class="col-sm-2">
                    <i class="fas fa-envelope"></i> @Html.DisplayNameFor(model => model.Email)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Email)
                </dd>

                <!-- Phone -->
                <dt class="col-sm-2">
                    <i class="fas fa-phone-alt"></i> @Html.DisplayNameFor(model => model.Phone)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Phone)
                </dd>

                <!-- CreatedAt -->
                <dt class="col-sm-2">
                    <i class="fas fa-calendar-check"></i> @Html.DisplayNameFor(model => model.CreatedAt)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.CreatedAt)
                </dd>
            </dl>
        </div>
    </div>

    <!-- Action Buttons -->
        <div class="d-flex justify-content-between align-items-center">
        <a asp-action="Edit" asp-route-id="@Model?.IdClient" class="btn-warning">
            <i class="fas fa-edit"></i> Edit
        </a> |
        <a asp-action="Index" class="btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>
</div>

</section>

<!-- Font Awesome CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

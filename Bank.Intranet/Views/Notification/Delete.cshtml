@model Company.Data.Data.Notification

@{
    ViewData["Title"] = "Delete";
}
<!-- Sekcja z tabelą z ograniczoną szerokością -->
<section class="table-section">
<div class="container mt-5">
    <h1 class="text-danger">
        <i class="fas fa-bell-slash"></i> Delete Notification
    </h1>
    <p class="lead">Are you sure you want to delete this notification? This action cannot be undone.</p>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">
                <i class="fas fa-exclamation-circle"></i> Notification Details
            </h4>
        </div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-heading"></i> @Html.DisplayNameFor(model => model.Title)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Title)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-comment-alt"></i> @Html.DisplayNameFor(model => model.Message)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Message)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-calendar-alt"></i> @Html.DisplayNameFor(model => model.NotificationDate)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.NotificationDate)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-envelope"></i> @Html.DisplayNameFor(model => model.Client)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Client.Email)
                </dd>
            </dl>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center">
        <form asp-action="Delete" class="d-inline">
            <input type="hidden" asp-for="IdNotification" />
            <button type="submit" class="btn-danger">
                <i class="fas fa-trash"></i> Delete
            </button>
        </form>
        <a asp-action="Index" class="btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>
</div>
</section>
<!-- Font Awesome CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

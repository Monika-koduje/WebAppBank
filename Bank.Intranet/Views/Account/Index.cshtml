@model IEnumerable<Company.Data.Data.Account>

@{
    ViewData["Title"] = "Index";
}

<!-- Sekcja z tabelą z ograniczoną szerokością -->
<section class="table-section">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="m-0">Accounts</h1>
        <p class="mb-0">
            <!-- Wyśrodkowany przycisk -->
            <a class="btn btn-primary" asp-action="Create">
                <i class="fas fa-plus-circle"></i>
            </a>
        </p>
    </div>
    <!-- Tabela -->
    <table class="table table-bordered table-striped table-hover">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.AccountNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Balance)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Client)
                </th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.AccountNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Balance)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Client.Email)
                    </td>
                    <td class="text-center">
                        <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.IdAccount">
                            <i class="fas fa-edit"></i> 
                        </a>
                        <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@item.IdAccount">
                            <i class="fas fa-eye"></i> 
                        </a>
                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.IdAccount" onclick="return confirm('Are you sure you want to delete this account?');">
                            <i class="fas fa-trash-alt"></i> 
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>


<section class="image">
    <img src="~/img/img5.jpg" />
</section>


<section class="icons-section">

    <!-- Accounts -->
    <a class="icon-link" asp-area="" asp-controller="Home" asp-action="Index" title="Home Page">
        <i class="fas fa-home"></i>
    </a>

    <a class="icon-link" asp-area="" asp-controller="Client" asp-action="Index" title="Clients">
        <i class="fas fa-user"></i>
    </a>
    <a class="icon-link" asp-area="" asp-controller="Transaction" asp-action="Index" title="Transactions">
        <i class="fas fa-exchange-alt"></i>
    </a>

</section>
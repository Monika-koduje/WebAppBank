@model Company.Data.Data.BankProduct

@{
    ViewData["Title"] = "Delete";
}

<!-- Sekcja z tabelą z ograniczoną szerokością -->
<section class="table-section">

<div class="container mt-5">
    <h1 class="text-danger">
        <i class="fas fa-trash-alt"></i> Delete Bank Product
    </h1>
    <p class="lead">Are you sure you want to delete this bank product? This action cannot be undone.</p>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">
                <i class="fas fa-exclamation-triangle"></i> Product Details
            </h4>
        </div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-tag"></i> @Html.DisplayNameFor(model => model.ProductName)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.ProductName)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-align-left"></i> @Html.DisplayNameFor(model => model.Description)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Description)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-th-list"></i> @Html.DisplayNameFor(model => model.ProductType)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.ProductType)
                </dd>
                <dt class="col-sm-3 text-secondary">
                    <i class="fas fa-percentage"></i> @Html.DisplayNameFor(model => model.InterestRate)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.InterestRate)
                </dd>
            </dl>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center">
        <form asp-action="Delete" class="d-inline">
            <input type="hidden" asp-for="IdProduct" />
            <button type="submit" class="btn-danger">
                <i class="fas fa-trash"></i> Delete
            </button>
        </form>
        <a asp-action="Index" class="btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>
</div>
</section>

<!-- Font Awesome CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
